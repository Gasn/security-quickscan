# FactoryOS advisory branch Check fixture

This documentation-only change tests review publication for a pull request
whose base branch does not require a `factory-review` Check. The repository's
protected `main` branch is unchanged.

The pull request is a release acceptance fixture and is not intended for merge.
